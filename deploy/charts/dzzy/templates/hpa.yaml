{{- include "dzzy.validate" . -}}
{{- if .Values.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dzzy.fullname" . }}
  labels:
    {{- include "dzzy.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dzzy.fullname" . }}
  minReplicas: {{ .Values.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.maxReplicas }}
  behavior:
    {{- toYaml .Values.autoscaling.behavior | nindent 4 }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}
  {{- if .Values.autoscaling.websocketMetricEnabled }}
    - type: Pods
      pods:
        metric:
          name: {{ .Values.autoscaling.websocketMetricName }}
        target:
          type: AverageValue
          averageValue: {{ .Values.autoscaling.targetWebsocketConnections | quote }}
  {{- end }}
{{- end }}
