name: dzzy

services:
  postgres:
    image: postgres:17-alpine
    container_name: dzzy-postgres
    environment:
      POSTGRES_USER: dzzy
      POSTGRES_PASSWORD: dzzy_dev
      POSTGRES_DB: dzzy
      PGDATA: /var/lib/postgresql/data
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - dzzy-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dzzy"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.dzzy.service: "postgres"
      com.dzzy.env: "dev"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: dzzy-redis
    ports:
      - "6379:6379"
    networks:
      - dzzy-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.dzzy.service: "redis"
      com.dzzy.env: "dev"
    restart: unless-stopped

  adminer:
    image: adminer:latest
    container_name: dzzy-adminer
    ports:
      - "8082:8080"
    networks:
      - dzzy-net
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    deploy:
      resources:
        limits:
          memory: 64m
    labels:
      com.dzzy.service: "adminer"
      com.dzzy.env: "dev"
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: dzzy-keycloak
    command: start-dev --http-port=8080
    environment:
      KC_DB: dev-mem
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "8180:8080"
    networks:
      - dzzy-net
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '200 OK'"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.dzzy.service: "keycloak"
      com.dzzy.env: "dev"
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    container_name: dzzy-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: dzzy_dev
      MINIO_ROOT_PASSWORD: dzzy_dev_secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    networks:
      - dzzy-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256m
    labels:
      com.dzzy.service: "minio"
      com.dzzy.env: "dev"
    restart: unless-stopped

  minio-init:
    image: minio/mc:latest
    container_name: dzzy-minio-init
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - dzzy-net
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 dzzy_dev dzzy_dev_secret &&
        mc mb --ignore-existing local/dzzy-media &&
        mc anonymous set none local/dzzy-media
      "
    labels:
      com.dzzy.service: "minio-init"
      com.dzzy.env: "dev"
    restart: "no"

  prometheus:
    image: prom/prometheus:latest
    container_name: dzzy-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - dzzy-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.dzzy.service: "prometheus"
      com.dzzy.env: "dev"
    restart: unless-stopped

volumes:
  postgres-data:
    name: dzzy-postgres-data
    labels:
      com.dzzy.volume: "postgres"
  minio-data:
    name: dzzy-minio-data
    labels:
      com.dzzy.volume: "minio"
  prometheus-data:
    name: dzzy-prometheus-data
    labels:
      com.dzzy.volume: "prometheus"

networks:
  dzzy-net:
    name: dzzy-net
    driver: bridge
    labels:
      com.dzzy.network: "dev"
